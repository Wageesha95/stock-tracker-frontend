export const SELL_COMMISSION_RATE = 0.0112;
export const SELL_COMMISSION_PCT = '1.12';

// Default annual rate, as a percentage, for the opportunity-cost calculation: what
// the money tied up in a holding could have earned in a savings account instead.
// Overridable per user in Settings; mirrors UserSettings.DEFAULT_OPPORTUNITY_COST_RATE
// on the backend, so change both together.
export const DEFAULT_OPPORTUNITY_COST_RATE = 6.5;
